---
interface Props {
	file: string,
    download_as: string,
    label?: string
}

const { file, download_as, label } = Astro.props;
let display_label = label ?? download_as;
---

<a href={`${file}?filename=${download_as}`}>{display_label}</a>
